/* 
 * 大皇帝上下行协议
 */

/**
 * 发送sock命令
 * @param {type} client
 * @param {type} cmd
 * @returns {undefined}
 */
function send(client, cmd) {

}

/**
 * 接收命令
 * @param {type} client
 * @param {type} buff
 * @returns {undefined}
 */
function receive(client, buff) {

}

/**
 * HTTP请求
 * @param {type} client
 * @param {type} cmd
 * @returns {undefined}
 */
function request(client, cmd) {

}


